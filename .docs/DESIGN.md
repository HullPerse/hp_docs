# {{PROJECT_NAME}} Design System

Источник правды по UI/UX (если применимо). Правила применяются к коду, новым компонентам и ревью.

<!-- 
Этот файл упрощённый по умолчанию. Для UI-проектов расширяй секции ниже.
Для не-UI проектов (CLI, бэкенд-only) можно оставить только общие принципы или удалить файл.
-->

## 1. Общие принципы

{{DESIGN_PRINCIPLES}}

<!--
Пример заполнения:
- Консистентность: одинаковые элементы выглядят и ведут себя одинаково.
- Простота: минимум визуального шума, чёткая иерархия.
- Доступность: все интерактивные элементы имеют focus, aria-подписи и состояния ошибок.
-->

## 2. Цвета и токены

{{COLOR_TOKENS}}

<!--
Пример заполнения:
| Токен | Значение | Назначение |
| --- | --- | --- |
| `--primary` | `#ffb858` | Основное действие |
| `--error` | `#e63946` | Ошибки |
Использовать семантические утилиты Tailwind, привязанные к токенам, а не хардкодить hex.
-->

## 3. Типографика

{{TYPOGRAPHY}}

<!--
Пример заполнения:
- Шрифт: JetBrains Mono, monospace, weight 600, размер 20px базовый.
- Кнопки и плотные элементы: text-sm font-extrabold.
-->

## 4. Компоненты

{{COMPONENTS}}

<!--
Пример заполнения:
### Button
- Variants: default (bg-primary), error, success, ghost, link.
- Sizes: sm, default, lg, icon.
- Props: loading, rendered, disabled.

### Input
- Типы: text, password (show/hide), number (стрелки), счётчик длины.
-->

## 5. Пространство и тени

{{SPACING}}

<!--
Пример заполнения:
- border-radius: 0 везде (глобальное правило).
- Границы: border-2 по умолчанию.
- Хард-тени: 4px 4px 0 0.
- Интервалы: шкала Tailwind 4px (gap-2, p-4, px-4).
-->

## 6. Движение и взаимодействие

{{MOTION}}

<!--
Пример заполнения:
- Короткие transition-colors и анимации fade/zoom в модалках (100ms).
- Ничего не анимирует layout.
- Загрузка -- спиннер/точки, не скелетоны.
-->

## 7. A11y и обязательные состояния

{{A11Y}}

<!--
Пример заполнения:
- Все интерактивные элементы с видимым focus (focus-visible:outline-primary).
- Иконки без текста -- sr-only подпись.
- Поддерживаются: loading, empty, error, disabled, dirty, stale состояния.
- Язык интерфейса -- русский.
-->

## 8. Правила

- Не создавать второй компонент, если существует аналог.
- Не хардкодить цвета и радиусы; использовать токены.
- Не менять глобальные правила без явного решения.
